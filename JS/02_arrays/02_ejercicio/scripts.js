console.log("conectando correctamente...");

/*
Enunciado:
Crea un arreglo con los días de
la semana. Luego, al hacer clic en
el botón, muestra con alert() el
día martes
usando su posición en el arreglo.
*/

function promedioTresPrimeros() {
    let numeros= [4,3,7,5,1];
    let promedio= (numeros[0]+numeros[1]+numeros[2])/3;
    alert("el promedio de los primero tres numeros es: \n"
    +promedio);
}